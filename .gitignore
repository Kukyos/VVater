# Local instructions — never pushed
CLAUDE.md

# Secrets
.env
.env.local
*.local

# Data cache — large NetCDF slabs, regenerable
data/cache/
*.nc
*.nc4

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Build output
submission/sih/final/*.bak
video/out/
# The film: clips regenerate from capture.cjs (about 1 GB), recorded voice stays local,
# and check.mjs writes the two JSON files the film reads
video/public/clips/
video/public/voice/
video/src/eval.json
video/src/voice.json

# Submission templates supplied by the portal, kept local
*.pptx
*.docx
# ...but the deck we build from it is ours
!submission/sih/final/*.pptx
dist/

# derived from the shots by figures.py
submission/sih/figures/crop-*.jpg
