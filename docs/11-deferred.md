# Deferred — knowingly incomplete, with what it blocks

Appended to in the same pass as the shortcut, never cleaned up at the end.

| ID | What | Blocks | What it would take |
|---|---|---|---|
| ~~D-01~~ | ~~Copernicus credentials.~~ **RESOLVED 2026-09-22.** Login verified, a 23 MB Bay of Bengal subset with `uo`/`vo` fetched. `sources.py` still raises — wiring the fetch is now ordinary work, not a blocker. | — | — |
| ~~D-02~~ | ~~No glider data.~~ **RESOLVED 2026-09-22.** 112 real casts in the box from the IOOS Glider DAC, co-located and giving the independent validation number. | — | — |
| D-02b | **EGO/Coriolis GDAC not wired.** 184 gliders over THREDDS — the archive the brief's dead FTP link served. No spatial index. | A second glider source, and the ability to say we read INCOIS's own cited archive. | Walk the 184 per-glider catalogues once, cache a lat/lon index. |
| D-08 | **Model fields have no depth-aware range test.** The published global range test catches 6 of 24 impossible cells; 18 between 35-40 degC pass it. | Silent rendering of physically impossible water. | A published source for depth-dependent limits. Logged in `10-unsourced.md` rather than invented. |
| D-03 | **`VoxelPrimitive` is `@experimental`** in CesiumJS — breaking changes without deprecation. | Nothing yet; will break silently on a Cesium bump. | `@cesium/engine` and `@cesium/widgets` are pinned exactly. Re-test the volume on every upgrade; voxel rendering fails silently, not loudly. |
| D-09 | **The two Cesium packages must be bumped together.** The `cesium` wrapper was dropped because its `@cesium/engine` range and the one `@cesium/widgets` wants disagreed, so npm nested a *second* engine. Two engines means two `ContextLimits`: the Viewer populates one, `VoxelPrimitive` reads the other, and the volume renders as nothing with no error. | Any dependency bump. | Bump both, then check `find node_modules -path "*@cesium/engine/package.json"` returns exactly one line. This cost most of a day. |
| D-10 | **Colourbar is a six-stop ramp evaluated in the shader**, not a 256-entry lookup texture. `CustomShader` texture uniforms need context limits that are not populated when a `VoxelPrimitive` first updates. | Fine fidelity on sharply non-linear palettes. | Raise the stop count (edit `RAMP_STOPS` and the GLSL together), or revisit the texture path if Cesium changes the ordering. |
| D-12 | **Copernicus requests are truncated to `config.COPERNICUS_MAX_DAYS` (2).** The default four-month window is 0.55 MB from INCOIS and roughly 1.8 GB from GLORYS. | Multi-day current animation. | Tile and cache the GLORYS path the way D-11 describes, then raise the clamp. |
| D-13 | **Streamlines are instantaneous, not trajectories.** They show the flow pattern in one analysis step, not where a particle would go over days. | Nothing claimed; the caveat ships in the API response and on screen. | Time-integrated advection through a sequence of daily fields, which is a different and much heavier feature. |
| D-14 | **`copernicusmarine` downloads log urllib3's `InsecureRequestWarning`** for its S3 host. Checked 2026-09-23: the toolbox only disables verification when `COPERNICUSMARINE_DISABLE_SSL_CONTEXT=True`, which is not set; and the same boto3/botocore stack, with our `truststore` injection, **rejects** `self-signed.badssl.com` and `expired.badssl.com` with `SSLError`. So verification is on and the warning is a false positive (urllib3 not recognising the truststore context). | Nothing. | Upstream, or silence that one warning by category once someone has re-run the badssl check. Never by disabling verification. |
| D-11 | **One tile, no streaming.** `OceanVoxelProvider` holds the whole volume in memory and `maximumTileCount` is 1. | Copernicus resolution (2.85 M voxels) and long time animations (L1). | Tile the provider and drive an LRU cache from the Cesium clock. |
| D-04 | **BGC / chlorophyll not ingested.** It lives in separate Argo `Sprof` files, not the core profile files. | The chlorophyll variable the brief names. | A second fetch path in `argo.py` against the `Sprof` index. |
| D-05 | **WCS not implemented**, only minimal WMS is planned. | An OGC claim we have chosen not to make (L8). | Nothing — this is deliberate. It stays here so it is never claimed by accident. |
| ~~D-06~~ | ~~TLS: INCOIS serves only its leaf certificate.~~ **RESOLVED 2026-09-23.** It did fail on Render (Linux). The intermediate, GlobalSign RSA OV SSL CA 2018, is bundled in `server/ocean/certs/` and appended to certifi; `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE` point at the result. `server/test_tls_bundle.py` checks it verifies with plain OpenSSL and that certifi alone does not. The intermediate expires 2028-11-21, and INCOIS may reissue under a different one before then. | — | — |
| D-07 | **Argo window fetch is 32 s cold** — 11 day-files at ~8.7 MB. | Interactive time-scrubbing over observations. | Fetch concurrently, or cache a slimmed region-only extract per day. |
| D-15 | **GLORYS12 is sent to the viewer as a 3x3 horizontal block mean** (265x217 -> 88x72, 36 levels untouched). Two million voxels in one tile froze the browser tab. Range-tested at native resolution first; the factor is in the provenance and on screen. | Seeing 1/12 deg structure (eddy edges) in 3D. The native field is still used for streamlines and WMS. | Tiling (D-11), then drop `volume.MAX_HORIZONTAL`. |
| D-16 | **Volume opacity is per 400 m of true depth, chosen by eye.** Per-sample opacity made finer grids denser (GLORYS rendered as a slab). 400 m is a display choice, not physics. The residual layer is exempt: it draws per bin. | Nothing claimed; opacity is a visual control. | A published transfer-function convention for ocean volumes, if one exists. |
| D-17 | **Graphics tiers were measured on one laptop** (integrated GPU, 1586x806 canvas): 12 / 23 / 34 / 60 fps from high to minimum. Auto-tune re-measures on every machine, so the tiers only set the order, but the steps between them are one machine's. | A tier that is too coarse or too fine a step on other hardware. | Measure on the demo machine and the INCOIS projector before the final. |
| D-18 | **`VoxelPrimitive.stepSize` is not a performance lever** here: Cesium stops at every voxel boundary regardless (13 -> 17 fps from 1x to 6x). Not offered in the UI. Cost is per pixel, so resolution is the lever. | Nothing; recorded so nobody re-adds the slider. | Upstream, or a coarser display grid. |
| D-19 | **The auto-tuner walks from "high" on every start**, about 5 s of visible quality stepping before it settles. | A smooth first impression on a slow machine. | Start the walk from the last auto result. |
| D-20 | **Uploaded text casts live in memory**, per server process, capped at 8 files of 10 MB. Restarting clears them. | Sharing an upload between users or sessions. | A cache directory and a delete endpoint. |
| D-21 | **Text QC is never interpreted.** Every text cast is data_mode 'U'; flag characters are carried for display only. | Accepting a CTD file's own QC. | Per-source flag vocabularies, declared by the uploader. |
| D-23 | **The global layer is surface temperature only.** The Globe view shows GLORYS12 at 0.49 m around the box; the volume, observations, residual and currents stay Bay of Bengal. Salinity and residual layers hide it rather than put a different quantity on the same colour scale. | A whole-ocean volume. | Tiling (D-11) plus a global Argo index; a different project in size. |
| D-24 | **Map 2D shows the section, not the volume.** Cesium's `VoxelPrimitive` has no 2D path, so the map draws the depth slice from the in-memory values; isosurface and opacity controls do not apply there. | Nothing the map could show. | None needed. |
| D-25 | **16 of 134 platforms (2 Argo floats, 14 glider casts) in the demo window have no analysis value at any of their levels** (masked coastal cells, or only above the grid's 5 m top), e.g. Argo 2902229 at 20.55 N 89.65 E. The profile chart draws the cast and prints why the analysis line is absent. Counted by hand against `/api/profile` on 2026-09-23, not by the harness, so not quoted in the deck. | Comparing near-coast casts. | Add the count to `run_eval.py` if it is ever quoted. |
| D-26 | **The assistant needs the network and a Groq key**, and the free tier allows 8,000 tokens a minute: a burst of questions hits 429. One retry is taken when the wait is under 12 s; otherwise the panel says so. The user guide is a tool, not part of the prompt, to stay under the limit. | An offline demo of the assistant; heavy use. | A paid tier, or a local model behind the same OpenAI-compatible call. |
| D-27 | **The number check is lexical.** A reply's numbers must appear (at the reply's precision) in a tool result or the question; small integers and years are exempt. A number derived correctly from two tool values (a difference) is flagged. Flagging a true number is the safe failure. | Nothing hidden; some true figures show the warning. | Have the tools return the derived quantities people ask for. |
| D-28 | **Interface actions from the assistant are a fixed whitelist** (view, mode, layer, global layer, depth, fly-to, open profile, 20 °C isotherm), clamped twice. Anything else is dropped silently. | Asking it to change the palette or range. | Add actions one at a time, each clamped in both places. |
| D-29 | **Seen from above in Fly, one slice at one colour range can read as a single colour** (the Bay at 93 m is 24–28 °C on a 2.7–29.2 range). The data is not wrong; the range is wide. | Contrast while flying. | Narrow Min/Max by hand; an auto-range to what is on screen would be a display choice to label. |
| D-30 | **GLORYS12 is still sent 3x3 block-meaned** (D-15). Rest-frame sharpening raised the resolution of what is drawn, not of the data; 2x2 was not measured against the tab freeze. | Finer eddies in the 3D Copernicus volume. | Measure 2x2 on the demo machine before changing `volume.MAX_HORIZONTAL`. |
| D-22 | **TCHP is per cast, not a map.** The analysis field could give a basin TCHP map (and a WMS layer); only the co-located comparison is built. | A TCHP layer on the globe. | `heat.tchp` over each grid column of the analysis; the function already takes one column. |
