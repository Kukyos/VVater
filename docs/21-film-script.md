# The film: the script to read

Generated by `node video/check.mjs` from `video/script.json`. **Do not edit here**; edit the
JSON and re-run. How the film is built, and the rules it keeps: `21-film.md`.

Runtime **6:33**. Each line has time to spare at 2.3 words a second. Record one file per
scene, named for the scene (`video/public/voice/tilt.wav`), and it plays from the start of that scene.

### 0:00 · `open` · 16 s

*On screen: cinema-open.*

> This is one real day of the world's ocean: its currents, and its winds, as the models saw them. Every tool built for this data shows you the same thing. The top.

### 0:16 · `title` · 6 s

*On screen: card: title.*

> This is VVater.

## The brief

### 0:22 · `brief` · 13 s

*On screen: card: quote.*

> INCOIS asked for one place where ocean models and real instruments can be seen together, in three dimensions, in a browser. With nothing to install.

## The block

### 0:35 · `tilt` · 21 s

*On screen: amphan-tilt.* Bay of Bengal · 14 May 2020 · two days before Cyclone Amphan formed.

> The Bay of Bengal, two days before Cyclone Amphan formed. From above, it is a map, like every ocean viewer. Tilt it, and it is a block of ocean standing on the sea surface, coloured at every depth the model has.

### 0:56 · `cut` · 22 s

*On screen: amphan-cut.* 35 native levels to 902.3 m · never more levels than the model has.

> Any side can be cut inwards, and the faces become sections through the inside. This is the warm upper layer a cyclone feeds on, and you can see how thin it is. 35 levels, the model's own, and never one more than it has.

## Floats inside the model

### 1:18 · `floats` · 19 s

*On screen: floats.*

> Real Argo floats stand inside the block, each one a line at the exact place it measured, from the surface to the depth it reached. These were in the water within 2 days of this model day.

### 1:37 · `float-click` · 25 s

*On screen: float-click.* 17 floats within ±2 days · 201 levels rejected by QC, drawn, not dropped.

> Click one, and it is compared against the model on the float's own day. Every level carries its quality flag, its data mode and the file it came from. Around Amphan, 17 floats reported, and 201 of their levels failed quality control. They are drawn as rejected. Never dropped.

## Before and after

### 2:02 · `amphan` · 17 s

*On screen: amphan-before, amphan-after.*

> Eight days later, two days after landfall. The same box, the same camera, the same colour bar. On average, the surface of this box is 0.77 degrees cooler.

## Anywhere, any day

### 2:19 · `anywhere` · 31 s

*On screen: arabian-sea, gulf-stream, el-nino, agulhas, drake.*

> And it is not only the Bay. Any box, anywhere on Earth, on any day from 1993 to 9 days ahead. 23 variables: temperature, salinity, currents, oxygen, chlorophyll, nutrients, carbon. Nothing is stored. Each block is read straight from Copernicus's own cloud stores, piece by piece.

## The whole ocean

### 2:50 · `globe` · 19 s

*On screen: globe.*

> Around the block, the whole planet is painted in the same variable, on the same colour bar, with that day's currents and winds flowing over it. A colour means the same value inside the block and out.

### 3:09 · `water` · 10 s

*On screen: cinema-water.*

> Or skim low over the water, with that day's currents running underneath.

## For fishermen

### 3:19 · `fishing` · 21 s

*On screen: fishing.*

> For fishermen: INCOIS's own potential fishing zone advisories, all 14 sectors, read exactly as published. Anywhere else, an indicative layer from sea surface fronts and chlorophyll, with the sea state. And it says, everywhere, that it is not an advisory.

## Where the model is unsure

### 3:40 · `volume` · 21 s

*On screen: bay-volume.* INCOIS Argo 10-day analysis · Bay of Bengal · August 2018.

> In the Bay of Bengal, INCOIS publishes its own analysis, with an error estimate for every cell. We draw it as a true volume, and water the model is unsure of is drawn faint, instead of as if it had been measured.

## Where the model is wrong

### 4:01 · `residual` · 17 s

*On screen: residual.* Observed minus model · 510 of 10,488 cells · 4.86 %.

> This is observed minus model, wherever an instrument actually measured. Only 4.86 percent of cells have any observation at all. The rest are left empty, not smoothed over.

### 4:18 · `bands` · 23 s

*On screen: card: bands.*

> Below 300 metres, the model and an independent glider agree to about a third of a degree. In the top 300 metres, the layer a cyclone forecaster cares about, the error is 2.89 degrees. One averaged number hides that. Drawing the column shows it.

## The model needs checking too

### 4:41 · `qc` · 25 s

*On screen: card: qc.*

> The model needs quality control too. The analysis has water at 44.42 degrees, 75 to 100 metres down. Argo's own published range test catches 6 of those cells, and we mask them. 18 more are impossible at that depth and still pass. We say so, instead of inventing a limit.

## For students

### 5:06 · `learn` · 19 s

*On screen: learn.*

> For students, a 6-lesson course on the real data: their own city's sea, the ocean's layers, the monsoon, Amphan, and how Argo floats are checked. Quiz answers are worked out from the block on the screen.

## Ask it

### 5:25 · `assistant` · 20 s

*On screen: assistant.*

> And an assistant that can drive the viewer. Ask for oxygen in the Arabian Sea, and it builds the block and shows you the control it used. Every number it says is checked against the data it read.

## Measured

### 5:45 · `evidence` · 14 s

*On screen: card: evidence.*

> Every number in this film comes from one test harness in the repository, which anyone can run with one command.

## Not yet

### 5:59 · `notyet` · 24 s

*On screen: card: notyet.*

> What is not done yet. Gliders are nearly absent here: of 2,562 datasets in the US glider archive, 1 flies inside the Bay. Europe's archive of 184 gliders is not connected yet. And a depth-aware range test needs a published limit, which is something INCOIS could give us.

### 6:23 · `end` · 10 s

*On screen: card: end.*

> VVater. Try it at v-vater dot vercel dot app.
